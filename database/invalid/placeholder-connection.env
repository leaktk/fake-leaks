# Metadata: label=Database connection with placeholders (invalid), type=database_connection, context=placeholder values, generator=manual, valid=false

# These are placeholder values and should NOT be flagged as real secrets

DATABASE_URL=postgresql://username:password@localhost:5432/database
DB_CONNECTION=mysql://user:pass@hostname:3306/dbname
MONGO_URI=mongodb://admin:password@localhost:27017/mydb

# Example placeholders
POSTGRES_URL=postgres://YOUR_USERNAME:YOUR_PASSWORD@your-host.com:5432/your_database
REDIS_URL=redis://:your-password-here@localhost:6379/0

# Documentation examples
# DATABASE_URL should be: postgresql://user:password@host:port/database
