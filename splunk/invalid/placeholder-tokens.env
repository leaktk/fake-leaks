# Metadata: label=Splunk placeholder credentials (invalid), type=api_key, provider=splunk, context=placeholder values, generator=manual, valid=false

# These are placeholder values and should NOT be flagged as real secrets

SPLUNK_HEC_TOKEN=your-hec-token-here
SPLUNK_HOST=your-splunk-host.com
SPLUNK_PASSWORD=your_password
SPLUNK_USERNAME=admin

# Documentation examples
SPLUNK_TOKEN=<HEC_TOKEN>
HEC_ENDPOINT=https://your-instance.splunkcloud.com:8088

# Example formats from documentation
# HEC Token format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx (UUID v4)
# Authentication token: JWT format bearer token
